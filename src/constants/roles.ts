// Default fallback role when no role is assigned
export const DEFAULT_ROLE = "CASHIER";

// Fallback color when role has no color in DB
export const DEFAULT_ROLE_COLOR = "bg-slate-100 text-slate-700";
